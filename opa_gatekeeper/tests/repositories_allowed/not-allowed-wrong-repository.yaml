apiVersion: v1
kind: Pod
metadata:
  name: mongo-not-allowed-2
spec:
  containers:
    - name: mongo
      image: vineguard/mongo:5.0
      args:
        - "run"
        - "--server"
        - "--addr=localhost:8080"
      resources:
        limits:
          cpu: "100m"
          memory: "30Mi"