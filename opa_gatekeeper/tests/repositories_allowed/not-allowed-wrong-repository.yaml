apiVersion: v1
kind: Pod
metadata:
  name: allowed
spec:
  containers:
    - name: opa
      image: vineguard/mongo:5.0
      args:
        - "run"
        - "--server"
        - "--addr=localhost:8080"
      resources:
        limits:
          cpu: "100m"
          memory: "30Mi"