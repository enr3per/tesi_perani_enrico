apiVersion: v1
kind: Pod
metadata:
  name: allowed
spec:
  containers:
    - name: opa
      image: perrea/mongo:v1
      args:
        - "run"
        - "--server"
        - "--addr=localhost:8080"
      resources:
        limits:
          cpu: "100m"
          memory: "30Mi"